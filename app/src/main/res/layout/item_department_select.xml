<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="com.shangyun.haile_manager_android.data.rule.SearchSelectRadioEntity" />

    </data>

    <androidx.appcompat.widget.AppCompatRadioButton
        android:layout_width="match_parent"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical"
        android:button="@null"
        android:text="@{item.selectName}"
        android:checked="@={item.getCheck}"
        android:background="@drawable/shape_bottom_stroke_dividing_efefef_mlr16"
        android:drawableEnd="@drawable/selector_login_agreement"
        style="@style/Font_16_333333"
        tools:text="店铺名称"
        android:layout_height="54dp" />
</layout>